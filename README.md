# DSL (Days Since Last)

To compile run `make` and the binary will be at `./build/dsl`

For a list of commands run `dsl -help`

Currently supports
- Adding events
- Removing events
- Updating events
- Clearing all events
